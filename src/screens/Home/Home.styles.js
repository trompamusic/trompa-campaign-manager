import { createStyles } from '@material-ui/styles';

export default () => createStyles({
  container: {
    display       : 'flex',
    alignItems    : 'center',
    justifyContent: 'center',
    height        : '50vh',
  },
  drawerContent: {},
});
