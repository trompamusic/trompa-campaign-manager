import { createStyles } from '@material-ui/styles';

export default () => createStyles({
  toolbarTop: {
    height        : 56,
    justifyContent: 'space-between',
    color         : '#005AA3',
  },
  logo: {
    width: 138,
  },
});
